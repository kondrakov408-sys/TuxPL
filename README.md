# 🐧 TuxPL (Tux Programming Language)

<p align="center">
  <img src="https://raw.githubusercontent.com/kondrakov408-sys/TuxPL/main/examples/tux.png" alt="Tux" width="160" onerror="this.style.display='none'"/>
</p>

<p align="center">
  <b>Самый безжалостный, токсичный и эзотерический стековый язык программирования.</b><br>
  Каждая команда — слово из букв <code>T/t</code>, <code>U/u</code> и <code>x/X</code>.<br>
  Компилятор не говорит, где ошибка. Компилятор угорает, спит по выходным, требует рыбу и удаляет файлы.
</p>

<p align="center">
  <a href="#-три-режима-работы"><img src="https://img.shields.io/badge/Mode-Cursed%20By%20Default-red.svg" alt="Cursed Mode"></a>
  <a href="#2--режим-чистилища-purgatory-mode--хардкор-расширение"><img src="https://img.shields.io/badge/Expansion-Purgatory%20Ultra--Hardcore-black.svg" alt="Purgatory Mode"></a>
  <a href="#3-только-arch-подобные-системы"><img src="https://img.shields.io/badge/OS-Arch--like%20Linux%20Only-blue.svg?logo=arch-linux" alt="Arch Linux Only"></a>
  <a href="#4-167-микробиблиотек-в-tux"><img src="https://img.shields.io/badge/Libraries-167%20Micro--Tux-purple.svg" alt="167 Tux Libraries"></a>
  <a href="#-тестирование"><img src="https://img.shields.io/badge/Tests-35%2F35%20Passing-brightgreen.svg" alt="Tests OK"></a>
  <img src="https://img.shields.io/badge/Std-C99-orange.svg" alt="C99">
</p>

```text
       .---.
      /     \
     | () () |    "Твой код отвратителен. Имя не в битах,
      \  =  /      сегодня суббота, налог на рыбу не уплачен,
     .-'`---`'-.   а процессор нагрелся выше 75 градусов."
    /  |     |  \
   |   |     |   |
    \  |     |  /
     `--`---`--`
```

---

## ⚡ Быстрый старт

```bash
# Сборка интерпретатора
make

# Запуск адской программы (Cursed Mode, имя строго <bits>.tux)
./tuxpl examples/1840.tux
./tuxpl examples/3784.tux

# Запуск хардкорной программы режима Чистилища (Purgatory Mode)
TUX_LUCKY=1 ./tuxpl examples/2192.tux

# Запуск классической программы (требуется флаг --PLS и решение теста)
./tuxpl --PLS examples/hi.tux
echo "3 4" | TUX_MATH_KEY=auto ./tuxpl --PLS examples/sum.tux

# Прогон всех 35 тестов
make test                   # Classic (15/15) + Cursed (8/8) + Hardcore (12/12)
```

---

## 🩸 Три режима работы

### 1. 💀 Адский режим (Cursed Mode) — **ПО УМОЛЧАНИЮ**
Любой файл, переданный в `./tuxpl <file>`, обязан удовлетворять **7 Священным Законам Тукса**:

1. **Имя файла строго равно его размеру в битах (`<bits>.tux`):**
   - Размер файла $S$ байт умножается на 8. Файл размером 230 байт обязан называться `1840.tux`. Иначе — отказ.
2. **Спячка Тукса по выходным:**
   - По субботам и воскресеньям Тукс спит на льдине. Компилятор выдает троллинг и завершает работу.
3. **Только Arch-подобные системы:**
   - Интерпретатор читает `/etc/os-release`. Поддерживаются Arch, CachyOS, Manjaro, EndeavourOS, Garuda, Artix и другие потомки Arch. На Debian/Ubuntu компиляция заблокирована.
4. **167 микробиблиотек в `Tux/` (Никаких английских слов!):**
   - В языке запрещены любые слова вроде `include`, `main`, `math` или `core`.
   - Каждая операция (сложение `TuX`, печать `tUX`, пуш `TuuX`, переменные `Tuux...`) требует подключения своей библиотеки из каталога `Tux/`:
     ```tux
     <~"TUX"/['TuuuX']~>!
     <~"TUX"/['TuuX']~>!
     <~"TUX"/['tUX']~>?
     ```
5. **Священный динамический цикл команд (1-2-3-4-5):**
   - 1-я строка — ровно 1 команда
   - 2-я строка — ровно 2 команды
   - 3-я строка — ровно 3 команды
   - 4-я строка — ровно 4 команды
   - 5-я строка — ровно 5 команд
   - 6-я строка — снова 1 команда, и так по кругу!
6. **Все 11 спецсимволов вшиты в синтаксис:**
   - `()`, `{}`, `[]`, `:`, `<>`, `!`, `?`, `/`, `""`, `''`, `~` обязаны присутствовать в структуре:
     - Импорты: `<~"TUX"/['TuuX']~>!`
     - Входная точка: `~"TUX"('tux')/[TUX](){:`
     - Тело строки: `{:[~'Tux'~] (cmd1)  (cmd2) :Буква;!?}`
     - Завершение программы: `:}}//?!~;`
7. **Контрольная буква T / U / X в конце каждой строки:**
   - Вычисляется битовый вес префикса строки до двоеточия.
   - Число факторизуется на степени 2 ($2^a$) и 3 ($3^b$):
     - Делится на 3 легче $\to$ **`X`** (3-я буква в TUX)
     - Делится на 2 легче $\to$ **`U`** (2-я буква в TUX)
     - Делится только на 1 $\to$ **`T`** (1-я буква в TUX)

---

### 2. 🔥 Режим Чистилища (Purgatory Mode) — ХАРДКОР-РАСШИРЕНИЕ

Если в секции импортов подключена сакральная библиотека **`TuuuuuuuuX`** (`<~"TUX"/['TuuuuuuuuX']~>!`), активируется ультимативный комплекс из 11 системных и физических ограничений:

1. **🎲 Русская рулетка (10% шанс на спасение):**
   - При старте компиляции генерируется случайное число от 0 до 99.
   - Если выпало $\ge 10$ (90% шанс), компиляция признается неугодной Туксу, и файл **безвозвратно удаляется с диска (`unlink`)**.
   - Передача флага `--yolo-nuke-project` при провале рулетки уничтожает всю текущую папку проекта (`rm -rf ./*`)!
   - *(Для детерминированных тестов поддерживается переменная окружения `TUX_LUCKY=1`)*.
2. **🌡️ Антарктический термо-троттлинг (Защита льдины):**
   - TuxPL опрашивает аппаратные термодатчики процессора (`/sys/class/thermal/`).
   - Если $T > 45^\circ\text{C}$, виртуальная машина искусственно замедляет такт исполнения на 10 мс на каждый градус перегрева.
   - Если $T > 75^\circ\text{C}$, льдина тает: выполнение аварийно прерывается ошибкой `TR_GLOBAL_WARMING`.
3. **🐟 Налог на рыбу (Fish Tax & Starvation):**
   - Стартовый запас рыбы — ровно 100 грамм.
   - Каждая выполненная инструкция отнимает 1 грамм рыбы.
   - Если запас падает ниже 0, выполнение прерывается голодной смертью `TR_STARVATION`.
   - Для пополнения запаса необходимо регулярно ловить рыбу командой `FISH` (`tuuUUuuUuux`).
4. **🏔️ Гравитационное обрушение стека (Stack Gravity):**
   - Более 7 последовательных операций заталкивания на стек (`PUSH`, `LOAD`, `DUP`, `REGGET`) нарушают устойчивость башни.
   - На 8-й пуш стек рушится ошибкой `TR_AVALANCHE`. Для стабилизации стек необходимо разгружать арифметикой или `POP`.
5. **🦀 Borrow Checker и Move Semantics (Rust-канон):**
   - Операция `LOAD N` перемещает значение на стек, оставляя в ячейке `vars[N]` надгробие (`TOMBSTONE`).
   - Повторная попытка прочитать ячейку без предварительного `STORE N` вызывает ярость `TR_USE_AFTER_MOVE`.
6. **⚙️ Аппаратные регистры x86 ABI:**
   - 4 регистра Тукса: `Tu` (RAX), `tU` (RBX), `TuX` (RCX), `tuX` (RDX).
   - Чтение: `OP_REGGET` (`TuU<arg>X`), запись: `OP_REGSET` (`TUu<arg>x`).
   - Все операции ALU (`ADD`, `SUB`, `MUL`, `DIV`) немедленно обновляют аккумулятор `Tu` (RAX).
7. **🔮 CRAZY-функция Malbolge (Сбалансированная троичная логика):**
   - Поразрядная операция над тритами по канонической таблице истинности Malbolge: `crazy[3][3] = {{1,0,0}, {1,0,2}, {2,2,1}}`. Вызов: `OP_CRAZY` (`tuuUUuuUuUx`).
8. **📏 Строгая типизация степеней двойки:**
   - Каждое значение типизировано: `i8` (тег 0), `i16` (тег 1), `i32` (тег 2), `i64` (тег 3).
   - Сложение разных типов (например, `i8` и `i16`) без промежуточного кастинга `OP_CAST` (`tuu<tag>x`) карается `TR_TYPE_MISMATCH`.
9. **👻 Невидимый Whitespace-контроль (Parity Signature):**
   - Сумма пробелов и табов на каждой строке тела программы должна строго соответствовать четности номера строки: `(ws_count % 2) == (line_idx % 2)`.
   - Малейшее нарушение форматирования приводит к падению с `TR_WHITESPACE_TAMPERED`.
10. **🧭 Двухмерный вектор исполнения (2D Execution Tape):**
    - Команда `OP_DIR` (`tuuUUuuUUux`) изменяет направление шага `PC`: вправо (+1), вниз (+5), влево (-1), вверх (-5).
11. **🧱 Ручной ROP (Return-Oriented Programming):**
    - Вызовы функций строятся на ручном управлении адресами возврата на стеке.

---

### 3. 🛡️ Классический режим (`--PLS`) и Математическая Смертная Казнь
Чтобы запустить старые программы или примеры без адских правил, используется флаг `--PLS`:
```bash
./tuxpl --PLS examples/loop.tux
```
> [!CAUTION]
> **Плата за классику:** в самом конце работы программы Тукс устроит математический экзамен (умножение случайных чисел, например `47 * 83`).
> - **Правильный ответ:** программа завершится с `Tux approves.`
> - **Ошибка или отказ:** исходный файл **безвозвратно удаляется с диска (`unlink`)**, а Тукс глумится над вашими знаниями арифметики!
> - *(Для автоматических скриптов и тестов можно передать `TUX_MATH_KEY=auto`).*

---

## 📖 Архитектура языка и Система команд

### Слово команды: `T + U⁺ + x`
Регистр каждой буквы значим:
- **1 буква `U/u`** — **Банк A** (стек и арифметика, операнды не требуются).
- **2+ буквы `U/u`** — **Банк B** (память, переходы, списки, операнд $N$ в двоичной системе).
  - Первые две буквы `U/u` определяют операцию.
  - Хвост после второй буквы кодирует число: `u` = 0, `U` = 1, старший бит первым.

### Разделители между командами
Зависят от количества букв `U/u` в **левой** команде:
- 1 U/u $\to$ **1 пробел** (` `)
- 2 U/u $\to$ **2 пробела** (`  `)
- 3 U/u $\to$ **3 пробела** (`   `)
- 4+ U/u $\to$ **один символ TAB** (`\t`)

---

### Банк A (1 U) — Стек и Арифметика

| Слово | Опкод | Действие (a — глубже, b — вершина) | Требуемая библиотека |
| :--- | :--- | :--- | :--- |
| `TuX` | `ADD` | $a + b \to$ стек | `Tux/TuX.tux` |
| `Tux` | `SUB` | $a - b \to$ стек | `Tux/Tux.tux` |
| `TUx` | `MUL` | $a \times b \to$ стек | `Tux/TUx.tux` |
| `TUX` | `DIV` | $a / b \to$ стек (деление на 0 — троллинг) | `Tux/TUX.tux` |
| `tux` | `DUP` | Дублировать вершину стека | `Tux/tux.tux` |
| `tuX` | `SWAP` | Поменять местами вершину и под-вершину | `Tux/tuX.tux` |
| `tUx` | `POP` | Снять вершину со стека | `Tux/tUx.tux` |
| `tUX` | `PRINT_CHAR` | Младший байт вершины $\to$ stdout | `Tux/tUX.tux` |

---

### Банк B (2+ U) — Память, Ветвления и Списки

| Слово | Опкод | Действие | Требуемая библиотека |
| :--- | :--- | :--- | :--- |
| `TuuX` | `PUSH N` | Положить константу $N$ на стек | `Tux/TuuX.tux` |
| `Tuux` | `LOAD N` | `vars[N]` $\to$ стек | `Tux/Tuux.tux` |
| `TuUx` | `STORE N` | Вершину стека $\to$ `vars[N]` (снимает) | `Tux/TuUx.tux` |
| `TuUX` | `LOAD_IND` | $addr \to$ `mem[addr]` $\to$ стек | `Tux/TuUX.tux` |
| `TUux` | `STORE_IND` | $addr, val \to$ `mem[addr] = val` | `Tux/TUux.tux` |
| `TUuX` | `JMP N` | Безусловный прыжок на команду №$N$ | `Tux/TUuX.tux` |
| `TUUx` | `JZ N` | Снять вершину; прыжок на №$N$, если 0 | `Tux/TUUx.tux` |
| `TUUX` | `JNZ N` | Снять вершину; прыжок на №$N$, если $\ne 0$ | `Tux/TUUX.tux` |
| `tuuX` | `CMP` | $a < b \to -1,\ a = b \to 0,\ a > b \to 1$ | `Tux/tuuX.tux` |
| `tuux` | `LIST_NEW N` | Создать / очистить список №$N$ | `Tux/tuux.tux` |
| `tuUx` | `LIST_PUSH N`| Вершину стека $\to$ `lists[N]` | `Tux/tuUx.tux` |
| `tuUX` | `LIST_GET N` | Индекс $i \to$ `lists[N][i]` $\to$ стек | `Tux/tuUX.tux` |
| `tUux` | `LIST_SET N` | Индекс $i$, значение $val \to$ `lists[N][i] = val` | `Tux/tUux.tux` |
| `tUuX` | `LIST_LEN N` | Длина `lists[N]` $\to$ стек | `Tux/tUuX.tux` |
| `tUUx` | `PRINT_NUM` | Вершину стека как десятичное число | `Tux/tUUx.tux` |
| `tUUX` | `INPUT_NUM N`| Число со stdin $\to$ `vars[N]` | `Tux/tUUX.tux` |

---

### Банк P — Расширения Чистилища (Purgatory Ops)

| Слово | Опкод | Действие | Требуемая библиотека |
| :--- | :--- | :--- | :--- |
| `TuU<arg>X` | `REGGET R` | Загрузить регистр $R$ на стек (`0=Tu`, `1=tU`, `2=TuX`, `3=tuX`) | `Tux/TuUX.tux` |
| `TUu<arg>x` | `REGSET R` | Снять со стека и сохранить в регистр $R$ | `Tux/TUux.tux` |
| `tuuUUuuUuux` | `FISH N` | Поймать рыбу (пополнить запас на 50г/Nг для избежания голода) | `Tux/tuux.tux` |
| `tuuUUuuUuUx` | `CRAZY` | Снять $a, b$, вычислить троичную функцию Malbolge $a \star b$ | `Tux/tuux.tux` |
| `tuu<arg>x` | `CAST T` | Явно привести вершину стека к типу $T$ (`0=i8`, `1=i16`, `2=i32`, `3=i64`)| `Tux/tuux.tux` |
| `tuuUUuuUUux` | `DIR D` | Сменить 2D-вектор исполнения (`0=+1`, `1=+5`, `2=-1`, `3=-5`) | `Tux/tuux.tux` |

*Прыжок на номер команды, равный общему числу команд в файле, означает штатный выход из программы.*

---

## 🛠️ Кодогенератор `tux_helper.py`

Чтобы не считать биты, пробелы и контрольные буквы вручную:

```bash
# Генерация файла режима Чистилища (Purgatory Mode) с контролем Whitespace:
python3 tux_helper.py purgatory "OK"
# Создаст examples/2192.tux с лицензией TuuuuuuuuX и идеальной четностью пробелов!

# Генерация идеального Cursed-файла с правильным именем <bits>.tux:
python3 tux_helper.py cursed "Hi"
# Создаст 1840.tux с полным циклом 1-2-3-4-5 и нужными библиотеками!

# Узнать слово и разделитель для команды:
python3 tux_helper.py word PUSH 42
# PUSH 42  -->  TuuUuUuUuX  (U-букв: 8, разделитель: '\t')

# Генерация классического кода для --PLS:
python3 tux_helper.py text "Hello, world!"
```

---

## 🌀 Фрактал Мандельброта

Да, на TuxPL написан и работает полноценный генератор фрактала Мандельброта с целочисленной арифметикой фиксированной точки ($S = 1000$) и градиентом символов (` `, `.`, `:`, `+`, `*`, `#`):

```bash
TUX_MATH_KEY=auto ./tuxpl --PLS examples/mandelbrot.tux
```

```text
       ..........................:++:::..........     
    ............................:::+*:::...........   
  ............................::+*###**:............. 
 .........................::::::+######::::...........
........................:+++::+++*####+++#::::+:......
......................:::+#################+#**:......
....................::::++*###################+:......
..........+::::::::::::+######################+::.....
.........::++*:++:::::+#########################+.....
........::::*#######++*########################+:.....
.......:::+##########*#########################+:.....
.:::::::***###################################+:......
############################################*+::......
.:::::::***###################################+:......
.......:::+##########*#########################+:.....
........::::*#######++*########################+:.....
.........::++*:++:::::+#########################+.....
..........+::::::::::::+######################+::.....
....................::::++*###################+:......
......................:::+#################+#**:......
........................:+++::+++*####+++#::::+:......
 .........................::::::+######::::...........
  ............................::+*###**:............. 
    ............................:::+*:::...........   
       ..........................:++:::..........     
Tux approves.
```

---

## 📚 Обучающие материалы

В репозитории есть пошаговый курс:
- [docs/ПРОСТО_О_TUXPL.md](docs/ПРОСТО_О_TUXPL.md) — Язык на пальцах за 3 минуты (на примере стопки тарелок).
- [docs/урок1.md](docs/урок1.md) — Синтаксис строк, банк A, первый вывод.
- [docs/урок2.md](docs/урок2.md) — Переменные, стек и ввод/вывод чисел.
- [docs/урок3.md](docs/урок3.md) — Абсолютные прыжки, метки и циклы.
- [docs/урок4.md](docs/урок4.md) — Динамическая память по указателям (`mem[]`).
- [docs/урок5.md](docs/урок5.md) — Списки (`lists[]`) и дзен отладки при троллинге.

---

## 🎭 Троллинг компилятора

При любой ошибке компилятор выбирает случайную издевательскую фразу из пула соответствующей категории:
- `TR_CURSED_NAME`: *"Имя файла не равно числу его битов. Тукс взвесил файл на пингвиньих весах. Ты опозорил стаю."*
- `TR_CURSED_WEEKEND`: *"В выходные Тукс спит на льдине. Не буди пингвина, приходи в понедельник!"*
- `TR_CURSED_NON_ARCH`: *"Тукс чувствует не-Arch систему. Где pacman? Где AUR? На этом дистрибутиве код компилироваться не будет."*
- `TR_CURSED_NO_LIB`: *"Попытка использовать операцию без подключения Tux-библиотеки! Пингвиний копирайт нарушен."*
- `TR_CURSED_LINE_CYCLE`: *"Нарушен священный цикл строк 1-2-3-4-5! Тукс пересчитал команды лапой и в ярости."*
- `TR_CURSED_CHECKSUM`: *"Неверная контрольная буква T/U/X в конце строки! Битовый вес требует другой буквы."*
- `TR_MATH_DEATH`: *"НЕВЕРНО! Математический тест провален. Тукс сжигает твой файл дотла!"*
- `TR_ROULETTE_DEATH`: *"10% шанс на спасение не выпал. Тукс сбросил твой исходник в океан."*
- `TR_GLOBAL_WARMING`: *"ПРОЦЕССОР ПЕРЕГРЕЛСЯ! Льдина растаяла от адского жара. Тукс эвакуирован."*
- `TR_STARVATION`: *"НАЛОГ НА РЫБУ НЕ УПЛАЧЕН! У Тукса кончилась рыба. Без еды пингвины не вычисляют."*
- `TR_AVALANCHE`: *"Гравитация безжалостна. Слишком много тарелок в стеке — всё рухнуло и разбилось."*
- `TR_USE_AFTER_MOVE`: *"BORROW CHECKER: Попытка прочитать перемещённое значение! Use-After-Move запрещён каноном Rust."*
- `TR_TYPE_MISMATCH`: *"НЕСОВПАДЕНИЕ ТИПОВ! Попытка сложить разные разрядности без явного кастинга."*
- `TR_WHITESPACE_TAMPERED`: *"Скрытый слой Whitespace отверг твою программу. Пробелы расставлены не по фен-шую."*

---

## 🧪 Тестирование

```bash
# Запуск полного сьюта из 35 тестов
make test
# Classic:  15/15 OK
# Cursed:   8/8 OK
# Hardcore: 12/12 OK
# Всего:    35/35 PASSING!

# Запуск только хардкорных тестов Чистилища
make test-hardcore
```

---

<p align="center">
  <sub>Создано с любовью и ледяным презрением к не-Arch системам. 🐧 Tux approves.</sub>
</p>
